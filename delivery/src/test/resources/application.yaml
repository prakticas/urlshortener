# Spring Datasource
spring:
  profiles:
    active: test,ValidatorReceiver,MainNode
  datasource:
    url: jdbc:hsqldb:mem:.
    username: sa
    password:
    driverClassName: org.hsqldb.jdbc.JDBCDriver
  jpa:
    open-in-view: false